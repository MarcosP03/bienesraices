extends ./layout/app

block contenido   
    section(class="py-5")
      h2(class="text-center text-4xl font-extrabold pt-10")= title

      if propiedades.length
        div(class="grid grid-cols-1 md:grid-cols-3 gap-5 mt-10")
          each propiedad in propiedades
            include ./templates/card.pug
      
      else
        p(class="text-center text-sm text-gray-800 py-10") No hay propiedades para este tipo de categoria.
